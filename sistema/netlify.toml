[build]
  # Railway deployment uses railway.toml, this file is for compatibility
  publish = "static"
  command = "pip install -r requirements.txt"

[build.environment]
  PYTHON_VERSION = "3.12"

[[redirects]]
  from = "/*"
  to = "/"
  status = 200
